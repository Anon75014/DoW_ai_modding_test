----------------------------------------
-- File: 'deathguardresearchktgmtactic.ai'
-- Edited by Thudmeizer		@ 08.05.2024

class 'DeathGuardResearchKtgmTactic' (BaseTactic)

DeathGuardResearchKtgm = {}

function DeathGuardResearchKtgmTactic:__init( base_ai ) super( base_ai )

	self:SetName("Death Guard Research Kill Team Tactic")

	-- The name of the squad to target
	self.squad1_name = "death_guard_squad_typhus_ktgm"
end

function DeathGuardResearchKtgmTactic:InitAbilities()
	
	-- Init abilities
	if (DeathGuardResearchKtgm.Strongheal_id == nil) then
		DeathGuardResearchKtgm.Strongheal_id = cpu_manager.stats:GetAbilityID( "ls_healing_1000" )
	end

	if (DeathGuardResearchKtgm.Lightheal_id == nil) then
		DeathGuardResearchKtgm.Lightheal_id = cpu_manager.stats:GetAbilityID( "ls_healing_500" )
	end
end

function DeathGuardResearchKtgmTactic:DoAbilities()
	
	aitrace("DeathGuardResearchKtgmTactic: Do abilities...")

	-- Check if squad is valid
    	for oSquad in military_manager:GetSquads() do
        	if oSquad:IsValid() then
            		if oSquad:GetSquadName() == self.squad1_name then

				-- Try to use Strong Heal ability on squad (restores target's health by 1000 HP)
				if (self.base_ai:CanDoAbility(DeathGuardResearchKtgm.Strongheal_id)) then
	
					-- Check if health needs an energency boost
					if (oSquad:WasRecentlyHurt() and oSquad:GetHealthPercentage() < 0.3) then

						-- Check if we have the needed resources
						local iPower = resource_manager:GetResourceAmount():Get( ResourceAmount.RT_Power )
						local iReq = resource_manager:GetResourceAmount():Get( ResourceAmount.RT_Requisition )
      						if iReq >= 100 and iPower >= 100 then		
		
							-- Use strong heal ability on commander
							self.base_ai:DoSpecialAbility(DeathGuardResearchKtgm.Strongheal_id)
							--print("Using Heavy Heal ability for player "..tostring(cpu_manager.player_id).."")
						end
					end
				end
-- Disabled light health to save on resources
--[[
				-- Try to use Light Heal ability on commander1 (restores target's health by 500 HP)
				if (self.base_ai:CanDoAbility(DeathGuardResearchKtgm.Lightheal_id)) then
	
					-- Check if health needs an energency boost
					if (oSquad:WasRecentlyHurt() and oSquad:GetHealthPercentage() > 0.7) then

						-- Check if we have the needed resources
						local iPower = resource_manager:GetResourceAmount():Get( ResourceAmount.RT_Power )
						local iReq = resource_manager:GetResourceAmount():Get( ResourceAmount.RT_Requisition )
      						if iReq >= 50 and iPower >= 50 then	
				
							-- Use light heal ability on commander
							self.base_ai:DoSpecialAbility(DeathGuardResearchKtgm.Lightheal_id)
							--print("Using Light Heal ability for player "..tostring(cpu_manager.player_id).."")
						end
					end
				end
]]
			end
		end
	end
end
